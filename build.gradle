allprojects {
    repositories {
        jcenter()
        mavenCentral()
    }
}

subprojects {
    ext {
        springBootVersion = '2.1.2.RELEASE'
        springCloudVersion = 'Greenwich.RELEASE'
    }

    group = 'edu.osumc.bmi.oauth2'
    version = '1.0.0'
}

